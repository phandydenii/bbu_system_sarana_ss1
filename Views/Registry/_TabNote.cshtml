@using BBU_SYSTEM.Models.Req
@model BBU_SYSTEM.ViewModel.RegisryDetailViewModel
@{
    var registryReq = new RegistryReq();
}

<div class="form-group clearfix">
    <div class="icheck-primary d-inline">
        <input type="checkbox" id="isShort" asp-for="@registryReq.Student!.IsReceivePhoto">
        <label for="isShort">Revieve</label>
    </div>
</div>

<label>Note</label>
<textarea type="text" rows="4" class="form-control"
          asp-for="@registryReq.Student!.Note" placeholder=""></textarea>